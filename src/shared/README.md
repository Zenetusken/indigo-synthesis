# Shared code

This folder is only for small technical primitives with no business ownership, such as
result types or test helpers. Domain concepts belong to a named module. It must not become
a global services, utilities, types, or state dumping ground.
